import { makeStyles } from '@mui/styles';

const TaskTableStyle = makeStyles({

    filterContainer : {
        alignItems : 'center',
        display : 'inline',
        textAlign : "end"
    },
    // activeButton : {
    //     backgro
    // }
})

export default TaskTableStyle